**/CLAUDE.local.md
__pycache__/
test/
**/.claude/settings.local.json

# Configuration with sensitive data
config.yaml
config.*.yaml

# Virtual environment
.venv/
venv/
env/
ENV/

# Python cache
*.py[cod]
*$py.class
*.so

# Output directories and logs
invoices/
*.log

# Generated accountant reports. These contain real counterparty names, NIPs
# and amounts. `invoices/output/` is already covered by `invoices/` above,
# but a non-default --directory would not be, so match the directory name
# and the workbook extensions wherever they appear.
output/
*.xlsx
*.xls

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
